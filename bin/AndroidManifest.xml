<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.trasselback.rapgenius"
    android:versionCode="7"
    android:versionName="2.3" >

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.trasselback.rapgenius.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.trasselback.rapgenius.LyricsActivity" >

            <!-- Links starting with explanation_clicked start a new intent -->
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />

                <action android:name="android.intent.action.VIEW" />

                <data android:scheme="explanation_clicked" />
            </intent-filter>
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />

                <action android:name="android.intent.action.VIEW" />

                <data android:scheme="song_clicked" />
            </intent-filter>
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />

                <action android:name="android.intent.action.VIEW" />

                <data android:scheme="fav_clicked" />
            </intent-filter>
        </activity>
        <activity android:name="com.trasselback.rapgenius.SearchActivity" >
        </activity>
        <activity
            android:name="com.trasselback.rapgenius.SettingsActivity"
            android:label="@string/title_activity_settings" >
        </activity>
        <activity
            android:name="com.trasselback.rapgenius.SettingsPreferenceActivity"
            android:label="@string/title_activity_settings" >
        </activity>
        <activity
            android:name="com.trasselback.rapgenius.MoreSongsActivity"
            android:label="@string/title_activity_more_songs" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.trasselback.rapgenius.LyricsActivity" />
        </activity>
    </application>

</manifest>